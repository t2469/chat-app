development:
  adapter: redis
  url: redis://redis:6379/1  # コンテナ名: redis
  channel_prefix: my_app_development

test:
  adapter: async

production:
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL") { "redis://redis:6379/1" } %>
  channel_prefix: my_app_production
